package com.exportApp.service.fileSystemExportService;

public class FileSystemExportService {
    //    private boolean createFolderForContainer(String containerName) {
//        File fileFolder = new File(currentDirectory, containerName);
//        if (!fileFolder.exists()) {
//            return fileFolder.mkdir();
//        }
//        return true;
//    }
    //    private void downloadFilesToFolder(BlobContainerClient blobContainerClient, String blobPath) throws IOException {
//        String filePath = currentDirectory + "/" + fileFolderName + "/" + blobPath;
//        if (createFolderForFiles()) {
//            BlobClient blobClient = blobContainerClient.getBlobClient(blobPath);
//            try {
//                blobClient.downloadToFile(filePath);
//            } catch (Exception e) {
//                throw new IOException("failed to download blob" + e);
//            }
//
//        }
//    }
}
